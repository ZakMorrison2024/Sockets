def network_action(message):  # This is a int command line, using the messaging system.
    if message == int:
        if message == 1:
           # do something
           pass
        if message == 2:
            # do something
           pass 
